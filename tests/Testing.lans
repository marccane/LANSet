constants
	real PI = 3.1415926535897932384626433832;
	enter set = 7;
	car caracteraso = 'O';
	car charespecial = '_';
	real testdefinitiu = 123;
	boolea boolOk = cert;
	boolea boolFals = fals;
	enter identificadorRedeclarat = 777;
fconstants

//============= Programa principal ================

programa Testing
variables
	car c;
	enter n;
	enter e;
	boolea unaIt;
	boolea b;

	real r1;
	real r2;
	real r3;

	enter i;
fvariables

r1 = 23.0;
r2 = 4.565;
r3 = r1 + r2 - (r1/2);
//r3 = r1 + r2;
escriureln(r3);

escriureln("Han de ser Cert:");
escriureln(3.0 > 2); //Ok
n = 23 - 1;
escriureln(n == 23);
escriureln(~1 == (1-2));

escriureln(2 == 2);
escriureln(2 == 2.0); //dona fals...
escriureln(2.0 == 2); //dona fals...
//escriureln(2.0 == 2.0);

escriureln("Han de ser Fals:");
escriureln(2 != 2); ////dona cert... error obvi
escriureln(2 != 2.0);
escriureln(2.0 != 2);

//escriureln(2.0 != 2.0);

i = 0;
mentre i < 10 fer
    escriureln(i);
    i = i+1;
fmentre

escriureln("DEBUGING");
escriureln( 1.0 == 1.0 );
escriureln( 0.25 == 0.25 );
escriureln( 1.25 == 1.25 );

/*
escriureln("Entra un real i t'hi sumo pi:");
llegir(r1);
escriureln(r1 + PI);
*/

c = '#';
//b = c > 7;

b = cert;
escriureln(b);

fprograma